# クラスと継承
# =======================
# 1) 空の`Movie()`クラスを定義します
# 2) `year`と`title`の2つの引数を取るダンダーinitメソッドを追加します
# 3) `Movie()`クラスを継承する`RomCom()`という名前のサブクラスを作成します
# 4) `ActionMovie()`という名前で`Movie()`クラスのサブクラスをもう1つ作成します。
#     このクラスは`Movie()`のダンダーinitメソッドを上書きして、デフォルトで
#     数字`13`に設定されている`pg`という別のインスタンス変数を追加します
# 5) これらのクラスの内容をさらに計画して、それを書き起こす練習をします。
#     ノートブックにメモしてください。`Movie()`クラスには他にどのような属性を
#     含められますか。どのようなメソッドがありますか。子クラスでは何をそのまま継承し、何を上書きしなければなりませんか
